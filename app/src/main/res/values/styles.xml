<resources>

    <!-- Application General Theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/dim_gray</item>
        <item name="android:navigationBarColor">@color/dark_gray</item>
    </style>

    <!-- Splash Activity Theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Navigation bar theme -->
    <style name="NavTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:textColorPrimary">@color/white</item>
    </style>
</resources>
